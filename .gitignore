# Secrets and local deployment config
config.json
.env
.env.*
*.pem
*.key
*.crt

# Local databases and WAL files
*.db
*.db-shm
*.db-wal
pos_replica.db

# Binaries and local runtime data
pos-server
pos-server.exe
cloudflared.exe
chrome-pos-profile/
POS_Simulator.exe

# Editor and OS files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Test artifacts and coverage
coverage.out
*.test

# git-filter-repo backup (contains old history with revoked tokens)
.git/filter-repo/
